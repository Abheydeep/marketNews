spring:
  datasource:
    url: jdbc:h2:mem:marketnarrative;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  cache:
    type: simple

app:
  security:
    jwt-secret: test-secret-test-secret-test-secret-test-secret
    issuer: market-narrative-test
    trading-admin-email: desk@marketnarrative.local
  demo-users:
    seed-demo-admin: true
    demo-admin-password: market-open
    seed-trading-admin: true
    trading-admin-password: market-open
